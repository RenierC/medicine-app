import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      light: "#3796a3",
      main: "#057c8c",
      dark: "#035662",
      contrastText: "#fff",
    },
  },
});

export default theme;
